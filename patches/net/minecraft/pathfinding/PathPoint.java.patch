--- ../src-base/minecraft/net/minecraft/pathfinding/PathPoint.java
+++ ../src-work/minecraft/net/minecraft/pathfinding/PathPoint.java
@@ -5,21 +5,23 @@
 import net.minecraftforge.fml.relauncher.Side;
 import net.minecraftforge.fml.relauncher.SideOnly;
 
+import java.util.concurrent.atomic.AtomicInteger;
+
 public class PathPoint
 {
     public final int x;
     public final int y;
     public final int z;
     private final int hash;
-    public int index = -1;
-    public float totalPathDistance;
-    public float distanceToNext;
-    public float distanceToTarget;
-    public PathPoint previous;
+    public AtomicInteger index = new AtomicInteger(-1);
+    public volatile float totalPathDistance;
+    public volatile float distanceToNext;
+    public volatile float distanceToTarget;
+    public volatile PathPoint previous;
     public boolean visited;
-    public float distanceFromOrigin;
-    public float cost;
-    public float costMalus;
+    public volatile float distanceFromOrigin;
+    public volatile float cost;
+    public volatile float costMalus;
     public PathNodeType nodeType = PathNodeType.BLOCKED;
 
     public PathPoint(int x, int y, int z)
@@ -33,7 +35,7 @@
     public PathPoint cloneMove(int x, int y, int z)
     {
         PathPoint pathpoint = new PathPoint(x, y, z);
-        pathpoint.index = this.index;
+        pathpoint.index.set(this.index.get());
         pathpoint.totalPathDistance = this.totalPathDistance;
         pathpoint.distanceToNext = this.distanceToNext;
         pathpoint.distanceToTarget = this.distanceToTarget;
@@ -95,7 +97,7 @@
 
     public boolean isAssigned()
     {
-        return this.index >= 0;
+        return this.index.get() >= 0;
     }
 
     public String toString()
